<UserControl x:Class="Y_Track.UI.VideosArea"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Y_Track.UI"
             mc:Ignorable="d">
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <!-- was Height="454" -->
            <StackPanel Visibility="Visible" x:Name="MiddleMessagePanel" DockPanel.Dock="Top" HorizontalAlignment="Center"  VerticalAlignment="Center" Height="400">
                <TextBlock  FontSize="30"  Foreground="LightGray" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,200,0,0">
                            Search Youtube Videos
                </TextBlock>
            </StackPanel>

        <Grid x:Name="VideosContainer" Visibility="Collapsed" ScrollViewer.CanContentScroll="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
            </Grid>

        </Grid>
</UserControl>
